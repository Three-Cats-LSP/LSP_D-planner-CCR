name: Notify Site on Push (disabled — archived repo)

# Legacy CCR repo is frozen at v2.30.31-final. Hub sync disabled.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "LSP_D-planner-CCR is archived; use LSP_D-planner-plus for site sync."
